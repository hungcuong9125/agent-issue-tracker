## Task graph

- Use one AIT issue per dispatched task; Lead owns dispatch claims, and a Peer updates and
  closes only its own issue. AIT lifecycle and ownership details live in the AIT skill.
- Classify by lifecycle, not by domain or phase: create a container only when a child needs
  it, and never re-file by moving an issue — see `framework/issue-policy.md`.